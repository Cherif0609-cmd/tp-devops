apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: lacets-app
spec:
  selector:
    app: lacets-api
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP